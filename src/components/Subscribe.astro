---
import DividerTop from "@assets/divider-top.astro";
import DividerBottom from "@assets/divider-bottom.astro";
import Button from "@components/ui/button.astro";
---

<section id="cta-1692">
    <DividerTop color="#f8f8f8" />
    <div class="cs-container">
        <div class="cs-content">
            <span class="cs-topper mb-2"
                >Experience the Power of Your Breath</span
            >
            <h2 class="cs-title">Free Breathwork Session</h2>
            <div class="h-2 w-28 bg-primary mb-16"></div>
            <p class="cs-text">
                Join our newsletter and receive access to a Free Breathwork
                Session. In just a few minutes, you'll experience the calming
                and transformative power of breathwork. This short session is
                designed to help you reduce stress, clear your mind, and
                reconnect with yourself—perfect for both beginners and seasoned
                practitioners. Sign up now to get started!
            </p>
            <form
                class="cs-form mt-4 max-w-xs"
                name="Contact Form"
                method="post"
                id="subscribe-form"
                action="/.netlify/edge-functions/submit-to-kit"
            >
                <div class="cs-input-group">
                    <input
                        class="cs-input"
                        type="text"
                        name="firstName"
                        placeholder="First name"
                        required
                    />
                    <input
                        class="cs-input"
                        type="email"
                        name="email"
                        placeholder="Enter your email"
                        required
                    />
                </div>
                <Button text="Subscribe" type="submit" class="w-full" />
            </form>
            <p
                id="success-message"
                class="cs-text pt-4 max-w-xs"
                style="display: none;"
            >
                Thank you for subscribing! Please check your email for the free
                session.
            </p>
            <p id="error-message" class="cs-text mt-4" style="display: none;">
                There was an error submitting the form. Please try again.
            </p>
        </div>
    </div>
    <DividerBottom />
</section>
<style lang="less">
    /*-- -------------------------- -->
<---            CTA             -->
<--- -------------------------- -*/

    /* Mobile - 360px */
    @media only screen and (min-width: 0rem) {
        #cta-1692 {
            padding: clamp(7.75rem, 10.82vw, 12.25rem) 1rem;
            background-color: var(--backgroundColor);
            position: relative;
            z-index: 1;
            overflow-x: hidden;

            .cs-container {
                width: 100%;
                max-width: (1280/16rem);
                padding: (32/16rem) 0;
                margin: auto;
                display: flex;
                flex-direction: column;
                align-items: center;
                /* 48px - 64px */
                gap: clamp(3rem, 6vw, 4rem);
            }

            .cs-content {
                /* set text align to left if content needs to be left aligned */
                text-align: center;
                width: 100%;
                max-width: (780/16rem);
                display: flex;
                flex-direction: column;
                /* centers content horizontally, set to flex-start to left align */
                align-items: center;
                z-index: 1;
            }

            .cs-title {
                max-width: 30ch;
            }

            .cs-topper {
                color: var(--headerColor);
            }

            .cs-text {
                /* 28px - 40px */
                margin: 0 0 clamp(1.75rem, 3vw, 2.5rem) 0;
                max-width: 80ch;
            }

            .cs-form {
                width: 100%;
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                /* 8px - 20px */
                gap: clamp(0.5rem, 2vw, 1.25rem);
            }

            .cs-input-group {
                display: flex;
                flex-direction: column;
                gap: clamp(0.5rem, 2vw, 1.25rem);
                width: 100%;
            }

            .cs-input {
                font-size: (16/16rem);
                width: 100%;
                height: clamp(3rem, 6.2vw, 4rem);
                margin: 0;
                /* prevents padding from adding to width and height */
                box-sizing: border-box;
                padding: 0;
                padding-left: (32/16rem);
                border: none;
                display: block;
                border-radius: 2rem;

                &::placeholder {
                    color: var(--bodyTextColor);
                }
            }

            .cs-button-solid {
                font-size: (16/16rem);
                font-weight: 400;
                /* 46px - 56px */
                line-height: clamp(2.875rem, 5.5vw, 3.5rem);
                text-align: center;
                text-decoration: none;
                min-width: (150/16rem);
                margin: 0;
                /* prevents padding from adding to the width */
                box-sizing: border-box;
                /* 32px - 48px */
                padding: 0 clamp(2rem, 4vw, 3rem);
                color: #fff;
                display: inline-block;
                position: relative;
                z-index: 1;

                &:before {
                    content: "";
                    width: 0%;
                    height: 100%;
                    background: #000;
                    opacity: 1;
                    position: absolute;
                    top: 0;
                    left: 0;
                    z-index: -1;
                    transition: width 0.3s;
                }

                &:hover {
                    &:before {
                        width: 100%;
                    }
                }
            }

            .cs-submit {
                width: 100%;
                border: none;

                &:hover {
                    cursor: pointer;
                }
            }

            .cs-graphic {
                position: absolute;
            }

            .cs-graphic-1 {
                /* 441px - 582px */
                width: clamp(27.5625rem, 42vw, 36.375rem);
                height: auto;
                top: 0;
                right: (-220/16rem);
            }

            .cs-graphic-2 {
                /* 371px - 582px */
                width: clamp(23.1875rem, 42vw, 36.375rem);
                height: auto;
                bottom: 0;
                left: (-118/16rem);
            }
        }
    }

    /* Tablet - 600px */
    @media only screen and (min-width: 37.5rem) {
        #cta-1692 {
            .cs-form {
                flex-direction: column;
            }
        }
    }

    /* Tablet - 768px */
    @media only screen and (min-width: 48rem) {
        #cta-1692 {
            .cs-submit {
                width: fit-content;
            }

            .cs-graphic-1 {
                right: (-129/16rem);
            }

            .cs-graphic-2 {
                left: (-126/16rem);
            }
        }
    }

    /* Dark Mode */
    @media only screen and (min-width: 0rem) {
        body.dark-mode {
            #cta-1692 {
                background-color: rgba(0, 0, 0, 0.2);

                .cs-title {
                    color: var(--bodyTextColorWhite);
                }

                .cs-text {
                    color: var(--bodyTextColorWhite);
                    opacity: 0.8;
                }

                .cs-input {
                    background-color: var(--medium);

                    &::placeholder {
                        color: var(--bodyTextColorWhite);
                    }
                }

                .cs-graphic {
                    opacity: 0.1;
                }
            }
        }
    }
</style>

<!-- <script>
    const form = document.querySelector("#subscribe-form") as HTMLFormElement;
    const successMessage = document.querySelector("#success-message");
    const errorMessage = document.querySelector("#error-message");

    form.addEventListener("submit", async (e) => {
        e.preventDefault();

        const formData = new FormData(form);
        const email = formData.get("email");
        const firstName = formData.get("firstName");

        console.log("email", email);
        console.log("firstName", firstName);

        try {
            const response = await fetch("/api/subscribe", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json",
                },
                body: JSON.stringify({
                    email,
                    firstName,
                }),
            });

            if (!response.ok) {
                throw new Error("Subscription failed");
            }

            const data = await response.json();

            if (data.success) {
                if (successMessage) {
                    successMessage.style.display = "block";
                    errorMessage!.style.display = "none";
                }
                form.reset();
            } else {
                throw new Error(data.error || "Subscription failed");
            }
        } catch (error) {
            console.error("Error:", error);
            if (errorMessage) {
                errorMessage.style.display = "block";
                successMessage!.style.display = "none";
            }
        }
    });
</script> -->
